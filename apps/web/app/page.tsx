import { prisma } from "@repo/db/client";

import ApplyPage from "./apply/pages";

export default async function Home() {

  // Create a new user
  

  return (
    <div>
       <ApplyPage />
    </div>
  )
}